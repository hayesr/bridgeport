<div class="container">
  <section id="<%=  @page.try(:slug)  %>">

    <% if @page.try(:regions) && @page.regions.any? %>
    <div class="row">
      <% @page.sorted_regions.each_with_index do |r, i| %>
      <div class="<%= "#{r.label} #{r.width}" %> columns">
        <%= raw r.body_with_links %>
      </div>
      <% end %>
    </div>
    <% end %>
  </section>
</div>